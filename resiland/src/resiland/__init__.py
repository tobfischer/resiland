from .resiland import(
    cantor, cant, create_cantor,
    GiveNumberOfElement, find_max, calculate_diff,
    sign_change, make_control_pars, create_network,
    jit_function, check_length_cantor, check_length_list, 
    initialize_amp, initialize_mu, initialize_var,
    symbol_dynamics, symbol_potential,
    potential, test_potential, potential_derivative,
    plot_potential, cantor_slope, noise_slope,
    Ornstein_Uhlenbeck, coupling_to_potential,
    white_noise, x_component, y_component,
    return_set_pa,
    prepare_jitcode, create_dynamic_variables
)

name = "resiland"